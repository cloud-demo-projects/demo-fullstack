# Maven build
# Junit unit test
# Cobertura code coverage
# Static code analysis-
# PMD
# Find Bugs
# Sonarcube

steps:
- task: Maven@3
  displayName: Maven Build
  inputs:
    mavenPomFile: '$(Build.SourcesDirectory)/${{ parameters.projectName }}/pom.xml'
    mavenOptions: '-Xmx3072m'
    javaHomeOption: 'JDKVersion'
    jdkVersionOption: '1.11'
    jdkArchitectureOption: 'x64'
    publishJUnitResults: true
    testResultsFiles: '**/surefire-reports/TEST-*.xml'
    goals: 'verify -Pdev'  

- task: Bash@3
  displayName: Logging
  inputs:
    targetType: 'inline'
    script: |
      ls -lrt $(Build.SourcesDirectory)/${{ parameters.projectName }}/
      ls -lrt $(Build.SourcesDirectory)/${{ parameters.projectName }}/target/

# - task: CopyFiles@2
#   displayName: Copy JAR
#   inputs:
#     SourceFolder: '$(System.DefaultWorkingDirectory)/${{ parameters.projectName }}/'
#     Contents: |
#       *.jar
#       **/target/**/?*.jar
#     TargetFolder: '$(Build.SourcesDirectory)'

# - task: Bash@3
#   displayName: Logging
#   inputs:
#     targetType: 'inline'
#     script: |
#       ls -lrt $(Build.SourcesDirectory)/${{ parameters.projectName }}/

- task: PublishBuildArtifacts@1
  displayName: Upload JAR
  inputs:
    PathtoPublish: '$(Build.SourcesDirectory)/target'
    ArtifactName: '$(Build.Repository.Name)'
    publishLocation: 'Container'
    
