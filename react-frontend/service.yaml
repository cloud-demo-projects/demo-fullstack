apiVersion: v1
kind: Service
metadata:
  labels:
    app: react-frontend
  name: react-frontend
spec:
  ports:
  - name: react-port
    port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: react-frontend
  type: LoadBalancer
status:
  loadBalancer: {}
